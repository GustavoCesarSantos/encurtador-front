// / <reference types="vite/client" />
interface ImportMeta {
    env: {
        VITE_BASE_URL: string;
        DOMAIN_URL: string;
        ACCESS_TOKEN: string;
        REFRESH_TOKEN: string;
    };
}
